import React from 'react';
import s from './Music.module.css';

const Music = (props) => {
    return (
        <main className={s.content}>
            Music...
        </main>
    );
}

export default Music;